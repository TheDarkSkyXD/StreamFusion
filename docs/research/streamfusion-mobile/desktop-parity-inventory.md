# Desktop parity inventory

This report is generated by `npm --prefix apps/desktop run parity:desktop:write`.
Run `npm --prefix apps/desktop run parity:desktop:check` to detect drift without writing.

Discovered structural facts: 130.
User-facing capabilities: 24.

## Playback: Block supported ads and retain safe stream playback when ad handling changes.

ID: `ad-blocking`.

### Entry

- `Player and Settings ad-block controls`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.
- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.

### Electron boundary

- `ipc-feature:adblock` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:adblock-handlers` in `apps/desktop/src/backend/ipc/handlers/adblock-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/adblock-store` in `apps/desktop/src/frontend/store/adblock-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/adblock/integration.test.ts`
- `apps/desktop/tests/components/player/twitch/twitch-live-player-adblock-indicator.test.tsx`

## App shell: Navigate the desktop layout, sidebar, window controls, and recovery surfaces.

ID: `app-shell-and-navigation`.

### Entry

- `Application shell`

### Renderer evidence

- `renderer-feature:shell` in `apps/desktop/src/frontend/features/shell`.

### Electron boundary

- `ipc-feature:app` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:app-handlers` in `apps/desktop/src/backend/ipc/handlers/app-handlers.ts`.

### Platform branches

- None.

### State evidence

- None.

### Persistence

- None.

### Verification

- `apps/desktop/tests/components/layout/AppLayout.test.tsx`
- `apps/desktop/tests/e2e/playbooks/00b-sidebar-navigation.playbook.md`

## Updates: Check, download, and install desktop application updates.

ID: `app-updates`.

### Entry

- `Settings update controls`

### Renderer evidence

- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.

### Electron boundary

- `ipc-feature:updates` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:update-handlers` in `apps/desktop/src/backend/ipc/handlers/update-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/update-store` in `apps/desktop/src/frontend/store/update-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/backend/ipc/handlers/update-handlers.test.ts`
- `apps/desktop/tests/backend/services/update-service.test.ts`

## Discovery: Browse categories and inspect category-specific streams, videos, and clips.

ID: `category-discovery`.

### Entry

- `/categories`
- `/categories/$platform/$categoryId`

### Renderer evidence

- `renderer-feature:discovery` in `apps/desktop/src/frontend/features/discovery`.
- `route:/categories` in `apps/desktop/src/frontend/routes/router.tsx`.
- `route:/categories/$platform/$categoryId` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:categories` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:videos` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:category-handlers` in `apps/desktop/src/backend/ipc/handlers/category-handlers.ts`.
- `ipc-handler:video-handlers` in `apps/desktop/src/backend/ipc/handlers/video-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:category-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/category-endpoints.ts`.
- `platform-endpoint:twitch:category-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/category-endpoints.ts`.

### State evidence

- `state-store:features/discovery/data/category-language-preference-store` in `apps/desktop/src/frontend/features/discovery/data/category-language-preference-store.ts`.

### Persistence

- `apps/desktop/src/frontend/features/discovery/data/category-language-preference-store.ts`

### Verification

- `apps/desktop/tests/backend/api/platforms/kick/kick-client.test.ts`
- `apps/desktop/tests/backend/api/platforms/twitch/twitch-client.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/category-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/stream-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/lazy-feature-loader.test.ts`
- `apps/desktop/tests/backend/ipc/video-handlers.test.ts`
- `apps/desktop/tests/pages/Categories.test.tsx`
- `apps/desktop/tests/pages/CategoryDetail.test.tsx`
- `apps/desktop/tests/store/category-language-preference-store.test.ts`

## Chat: Replay available Twitch and Kick chat alongside on-demand video.

ID: `chat-replay`.

### Entry

- `Video chat replay controls`

### Renderer evidence

- `renderer-feature:chat` in `apps/desktop/src/frontend/features/chat`.
- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.

### Electron boundary

- `ipc-feature:chat-replay` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:chat-replay-handlers` in `apps/desktop/src/backend/ipc/handlers/chat-replay-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:features/chat/data/chat-replay-playback-store` in `apps/desktop/src/frontend/features/chat/data/chat-replay-playback-store.ts`.

### Persistence

- None.

### Verification

- `apps/desktop/tests/backend/services/chat-replay-service.test.ts`
- `apps/desktop/tests/components/chat-replay/chat-replay-session.test.tsx`

## Support: Inspect diagnostics and logs, then create a bug report with relevant evidence.

ID: `diagnostics-logs-and-bug-reports`.

### Entry

- `Bug report controls`
- `Settings diagnostics workspace`

### Renderer evidence

- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.

### Electron boundary

- `ipc-feature:bug-reports` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:diagnostics` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:logs` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:bug-report-handlers` in `apps/desktop/src/backend/ipc/handlers/bug-report-handlers.ts`.
- `ipc-handler:diagnostics-handlers` in `apps/desktop/src/backend/ipc/handlers/diagnostics-handlers.ts`.
- `ipc-handler:log-handlers` in `apps/desktop/src/backend/ipc/handlers/log-handlers.ts`.

### Platform branches

- None.

### State evidence

- None.

### Persistence

- `apps/desktop/src/backend/logging/log-paths.ts`

### Verification

- `apps/desktop/tests/backend/ipc/handlers/bug-report-handlers.test.ts`
- `apps/desktop/tests/pages/Settings/diagnostics-workspace.test.tsx`

## Media library: Download, retry, remove, reveal, and open local video and clip files.

ID: `downloads`.

### Entry

- `/downloads`

### Renderer evidence

- `renderer-feature:media-library` in `apps/desktop/src/frontend/features/media-library`.
- `route:/downloads` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:downloads` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:download-handlers` in `apps/desktop/src/backend/ipc/handlers/download-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/download-duplicate-confirmation-store` in `apps/desktop/src/frontend/store/download-duplicate-confirmation-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/download-queue-service.ts`

### Verification

- `apps/desktop/tests/pages/Downloads.delete-dialog.test.tsx`
- `apps/desktop/tests/pages/Downloads.test.tsx`

## Chat: Load, render, and configure platform and third-party emotes and cosmetics.

ID: `emotes-and-cosmetics`.

### Entry

- `Chat panel and emote picker`

### Renderer evidence

- `renderer-feature:chat` in `apps/desktop/src/frontend/features/chat`.

### Electron boundary

- `ipc-feature:emotes` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:emote-handlers` in `apps/desktop/src/backend/ipc/handlers/emote-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/chat-cosmetics-store` in `apps/desktop/src/frontend/store/chat-cosmetics-store.ts`.
- `state-store:store/emote-store` in `apps/desktop/src/frontend/store/emote-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/backend/services/emotes/emote-manager.test.ts`
- `apps/desktop/tests/components/chat/EmotePickerPopover.test.tsx`

## Discovery: See followed live streams, follow channels, and synchronize account follows into the local list.

ID: `followed-streams-and-sync`.

### Entry

- `/following`
- `Follow controls`

### Renderer evidence

- `renderer-feature:auth` in `apps/desktop/src/frontend/features/auth`.
- `renderer-feature:discovery` in `apps/desktop/src/frontend/features/discovery`.
- `route:/following` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:auth` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:channels` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:storage` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:streams` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:auth-handlers` in `apps/desktop/src/backend/ipc/handlers/auth-handlers.ts`.
- `ipc-handler:channel-handlers` in `apps/desktop/src/backend/ipc/handlers/channel-handlers.ts`.
- `ipc-handler:storage-handlers` in `apps/desktop/src/backend/ipc/handlers/storage-handlers.ts`.
- `ipc-handler:stream-handlers` in `apps/desktop/src/backend/ipc/handlers/stream-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:follow-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/follow-endpoints.ts`.
- `platform-endpoint:twitch:follow-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/follow-endpoints.ts`.

### State evidence

- `state-store:store/follow-store` in `apps/desktop/src/frontend/store/follow-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/backend/api/platforms/kick/kick-client.test.ts`
- `apps/desktop/tests/backend/api/platforms/twitch/twitch-client.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/auth-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/auth-security-boundary.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/channel-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/storage-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/stream-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/lazy-feature-loader.test.ts`
- `apps/desktop/tests/backend/services/storage-service.test.ts`
- `apps/desktop/tests/hooks/queries/useLiveFavoriteStreams.test.tsx`
- `apps/desktop/tests/pages/Following.test.tsx`
- `apps/desktop/tests/store/follow-store.test.ts`
- `packages/core/tests/follows.test.mjs`

## Settings: Configure general behavior, chat behavior, and theme preferences.

ID: `general-chat-and-theme-preferences`.

### Entry

- `/settings`

### Renderer evidence

- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.
- `route:/settings` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:storage` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:system` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:storage-handlers` in `apps/desktop/src/backend/ipc/handlers/storage-handlers.ts`.
- `ipc-handler:system-handlers` in `apps/desktop/src/backend/ipc/handlers/system-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/app-store` in `apps/desktop/src/frontend/store/app-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/components/settings/ChatSettingsSection.test.tsx`
- `apps/desktop/tests/pages/Settings.test.tsx`

## Discovery: Browse live streams, channel cards, and current platform availability from Home.

ID: `home-live-discovery`.

### Entry

- `/`

### Renderer evidence

- `renderer-feature:discovery` in `apps/desktop/src/frontend/features/discovery`.
- `route:/` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:channels` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:platform-health` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:streams` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:user-profile` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:channel-handlers` in `apps/desktop/src/backend/ipc/handlers/channel-handlers.ts`.
- `ipc-handler:platform-health-handlers` in `apps/desktop/src/backend/ipc/handlers/platform-health-handlers.ts`.
- `ipc-handler:stream-handlers` in `apps/desktop/src/backend/ipc/handlers/stream-handlers.ts`.
- `ipc-handler:user-profile-handlers` in `apps/desktop/src/backend/ipc/handlers/user-profile-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:channel-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/channel-endpoints.ts`.
- `platform-endpoint:kick:stream-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/stream-endpoints.ts`.
- `platform-endpoint:kick:user-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/user-endpoints.ts`.
- `platform-endpoint:twitch:channel-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/channel-endpoints.ts`.
- `platform-endpoint:twitch:stream-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/stream-endpoints.ts`.
- `platform-endpoint:twitch:user-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/user-endpoints.ts`.

### State evidence

- `state-store:hooks/network-status-store` in `apps/desktop/src/frontend/hooks/network-status-store.ts`.

### Persistence

- None.

### Verification

- `apps/desktop/tests/backend/api/platforms/kick/kick-client.test.ts`
- `apps/desktop/tests/backend/api/platforms/twitch/twitch-client.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/channel-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/stream-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/lazy-feature-loader.test.ts`
- `apps/desktop/tests/hooks/queries/useStreams.test.tsx`
- `apps/desktop/tests/pages/Home.test.tsx`

## Chat: Read and send Twitch and Kick live chat messages with room state and subscriber eligibility.

ID: `live-chat`.

### Entry

- `Stream chat panel`

### Renderer evidence

- `renderer-feature:chat` in `apps/desktop/src/frontend/features/chat`.

### Electron boundary

- `ipc-feature:chat` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:chat-eligibility` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:kick-chat` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:chat-eligibility-handlers` in `apps/desktop/src/backend/ipc/handlers/chat-eligibility-handlers.ts`.
- `ipc-handler:chat-handlers` in `apps/desktop/src/backend/ipc/handlers/chat-handlers.ts`.
- `ipc-handler:kick-chat-handlers` in `apps/desktop/src/backend/ipc/handlers/kick-chat-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:chat-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/chat-endpoints.ts`.
- `platform-endpoint:twitch:chat-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/chat-endpoints.ts`.

### State evidence

- `state-store:store/chat-store` in `apps/desktop/src/frontend/store/chat-store.ts`.
- `state-store:store/persisted-chat-history` in `apps/desktop/src/frontend/store/persisted-chat-history.ts`.
- `state-store:store/room-state-store` in `apps/desktop/src/frontend/store/room-state-store.ts`.
- `state-store:store/user-popout-chat-context` in `apps/desktop/src/frontend/store/user-popout-chat-context.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/components/chat/ChatPanel.test.tsx`
- `apps/desktop/tests/components/chat/KickChat.test.tsx`

## Playback: Open and watch live Twitch and Kick streams.

ID: `live-playback`.

### Entry

- `/stream/$platform/$channel`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.
- `route:/stream/$platform/$channel` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:playback` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- None.

### Platform branches

- None.

### State evidence

- `state-store:store/reconnect-dialog-store` in `apps/desktop/src/frontend/store/reconnect-dialog-store.ts`.

### Persistence

- None.

### Verification

- `apps/desktop/tests/hooks/useStreamPlayback.network-recovery.test.tsx`
- `apps/desktop/tests/pages/Stream.test.tsx`

## Playback: Download a local caption model and show live captions for supported playback.

ID: `local-captions`.

### Entry

- `Player caption controls`
- `Settings caption controls`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.
- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.

### Electron boundary

- `ipc-feature:local-captions` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:local-caption-handlers` in `apps/desktop/src/backend/ipc/handlers/local-caption-handlers.ts`.

### Platform branches

- None.

### State evidence

- None.

### Persistence

- `apps/desktop/src/backend/services/captions/caption-model-store.ts`

### Verification

- `apps/desktop/tests/backend/services/captions/caption-model-store.test.ts`
- `apps/desktop/tests/components/player/local-caption-presentation.test.tsx`

## Moderation: Manage moderated channels, users, timeouts, and local moderation logs.

ID: `moderation`.

### Entry

- `/mod`
- `/mod/kick/$channel`
- `/mod/twitch/$channel`

### Renderer evidence

- `renderer-feature:moderation` in `apps/desktop/src/frontend/features/moderation`.
- `route:/mod` in `apps/desktop/src/frontend/routes/router.tsx`.
- `route:/mod/kick/$channel` in `apps/desktop/src/frontend/routes/router.tsx`.
- `route:/mod/twitch/$channel` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:mod-log` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:timeout-moderation` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:twitch-api` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:modlog-handlers` in `apps/desktop/src/backend/ipc/handlers/modlog-handlers.ts`.
- `ipc-handler:timeout-moderation-handlers` in `apps/desktop/src/backend/ipc/handlers/timeout-moderation-handlers.ts`.
- `ipc-handler:twitch-api-handlers` in `apps/desktop/src/backend/ipc/handlers/twitch-api-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:features/moderation/data/moderated-channels-store` in `apps/desktop/src/frontend/features/moderation/data/moderated-channels-store.ts`.
- `state-store:store/dev-mod-override-store` in `apps/desktop/src/frontend/store/dev-mod-override-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/pages/Mod/channel/ModChannelPage.test.tsx`
- `apps/desktop/tests/pages/Mod/index.test.tsx`

## Multi-stream: Build and manage a multi-stream viewing layout.

ID: `multistream`.

### Entry

- `/multistream`

### Renderer evidence

- `renderer-feature:multistream` in `apps/desktop/src/frontend/features/multistream`.
- `route:/multistream` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- None.

### Main-process evidence

- None.

### Platform branches

- None.

### State evidence

- `state-store:features/multistream/data/multistream-store` in `apps/desktop/src/frontend/features/multistream/data/multistream-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/components/multistream/add-stream-dialog.test.tsx`
- `apps/desktop/tests/pages/MultiStream.test.tsx`

## Notifications: Receive live notifications and open the matching stream from the notification action.

ID: `notifications`.

### Entry

- `Desktop live notification`
- `Notification preferences`

### Renderer evidence

- `renderer-feature:shell` in `apps/desktop/src/frontend/features/shell`.

### Electron boundary

- `ipc-feature:notifications` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- None.

### Platform branches

- None.

### State evidence

- `state-store:store/notification-store` in `apps/desktop/src/frontend/store/notification-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/App.live-notification-router.integration.test.tsx`
- `apps/desktop/tests/hooks/use-live-notification-bridge.test.tsx`

## Accounts: Connect, validate, refresh, and disconnect Twitch and Kick accounts.

ID: `platform-account-auth`.

### Entry

- `Login dialog`
- `Settings account controls`

### Renderer evidence

- `renderer-feature:auth` in `apps/desktop/src/frontend/features/auth`.

### Electron boundary

- `ipc-feature:auth` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:token-status` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:auth-handlers` in `apps/desktop/src/backend/ipc/handlers/auth-handlers.ts`.
- `ipc-handler:token-status-handlers` in `apps/desktop/src/backend/ipc/handlers/token-status-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/auth-store` in `apps/desktop/src/frontend/store/auth-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/components/auth/LoginDialog.test.tsx`
- `apps/desktop/tests/hooks/useAuth.test.tsx`

## Playback: Control playback quality, volume, fullscreen, mini player, and picture-in-picture behavior.

ID: `player-controls-and-pip`.

### Entry

- `Live and VOD player controls`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.

### Electron boundary

- `ipc-feature:slots` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:slot-controller-handlers` in `apps/desktop/src/backend/ipc/handlers/slot-controller-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:store/pip-store` in `apps/desktop/src/frontend/store/pip-store.ts`.
- `state-store:store/volume-store` in `apps/desktop/src/frontend/store/volume-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/components/player/hooks/use-picture-in-picture.test.ts`
- `apps/desktop/tests/components/player/player-controls.test.tsx`

## Settings: Configure a stream proxy and understand connection state.

ID: `proxy-and-connectivity`.

### Entry

- `Connection status`
- `Settings proxy controls`

### Renderer evidence

- `renderer-feature:settings` in `apps/desktop/src/frontend/features/settings`.

### Electron boundary

- `ipc-feature:connectivity` in `apps/desktop/src/shared/ipc-channels.ts`.
- `ipc-feature:proxy` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:connectivity-handlers` in `apps/desktop/src/backend/ipc/handlers/connectivity-handlers.ts`.
- `ipc-handler:proxy-handlers` in `apps/desktop/src/backend/ipc/handlers/proxy-handlers.ts`.

### Platform branches

- None.

### State evidence

- `state-store:hooks/network-status-store` in `apps/desktop/src/frontend/hooks/network-status-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/backend/ipc/handlers/connectivity-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/proxy-handlers.test.ts`

## Discovery: Search channels, streams, videos, clips, and categories across supported platforms.

ID: `search`.

### Entry

- `/search`
- `Top navigation search`

### Renderer evidence

- `renderer-feature:discovery` in `apps/desktop/src/frontend/features/discovery`.
- `route:/search` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:search` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:search-handlers` in `apps/desktop/src/backend/ipc/handlers/search-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:search-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/search-endpoints.ts`.
- `platform-endpoint:twitch:search-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/search-endpoints.ts`.

### State evidence

- None.

### Persistence

- None.

### Verification

- `apps/desktop/tests/backend/api/platforms/kick/kick-client.test.ts`
- `apps/desktop/tests/backend/api/platforms/twitch/twitch-client.test.ts`
- `apps/desktop/tests/backend/ipc/handlers/search-handlers.test.ts`
- `apps/desktop/tests/backend/ipc/lazy-feature-loader.test.ts`
- `apps/desktop/tests/backend/search/focused-search-sources.test.ts`
- `apps/desktop/tests/components/TopNavBar/SearchBar.router.integration.test.tsx`
- `apps/desktop/tests/pages/SearchResults.test.tsx`

## Playback: Record a live stream, recover interrupted recording work, and open completed files.

ID: `stream-recording`.

### Entry

- `Live player recording controls`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.

### Electron boundary

- `ipc-feature:stream-recording` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:stream-recording-handlers` in `apps/desktop/src/backend/ipc/handlers/stream-recording-handlers.ts`.

### Platform branches

- None.

### State evidence

- None.

### Persistence

- `apps/desktop/src/backend/services/stream-recording-session-store.ts`

### Verification

- `apps/desktop/tests/backend/services/stream-recording-recovery.test.ts`
- `apps/desktop/tests/components/recording/stream-recording-control.test.tsx`

## Playback: Open and seek Twitch and Kick VODs and clips.

ID: `vod-and-clip-playback`.

### Entry

- `/video/$platform/$videoId`

### Renderer evidence

- `renderer-feature:playback` in `apps/desktop/src/frontend/features/playback`.
- `route:/video/$platform/$videoId` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- `ipc-feature:videos` in `apps/desktop/src/shared/ipc-channels.ts`.

### Main-process evidence

- `ipc-handler:video-handlers` in `apps/desktop/src/backend/ipc/handlers/video-handlers.ts`.

### Platform branches

- `platform-endpoint:kick:clip-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/clip-endpoints.ts`.
- `platform-endpoint:kick:video-endpoints` in `apps/desktop/src/backend/api/platforms/kick/endpoints/video-endpoints.ts`.
- `platform-endpoint:twitch:clip-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/clip-endpoints.ts`.
- `platform-endpoint:twitch:video-endpoints` in `apps/desktop/src/backend/api/platforms/twitch/endpoints/video-endpoints.ts`.

### State evidence

- `state-store:store/playback-position-store` in `apps/desktop/src/frontend/store/playback-position-store.ts`.
- `state-store:store/seek-interval-store` in `apps/desktop/src/frontend/store/seek-interval-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/backend/api/platforms/kick/kick-client.test.ts`
- `apps/desktop/tests/backend/api/platforms/twitch/twitch-client.test.ts`
- `apps/desktop/tests/backend/ipc/video-handlers.test.ts`
- `apps/desktop/tests/components/player/vod-seek-controls.test.tsx`
- `apps/desktop/tests/pages/Video.test.tsx`

## Media library: Review and manage local watch history.

ID: `watch-history`.

### Entry

- `/history`

### Renderer evidence

- `renderer-feature:media-library` in `apps/desktop/src/frontend/features/media-library`.
- `route:/history` in `apps/desktop/src/frontend/routes/router.tsx`.

### Electron boundary

- None.

### Main-process evidence

- None.

### Platform branches

- None.

### State evidence

- `state-store:store/history-store` in `apps/desktop/src/frontend/store/history-store.ts`.

### Persistence

- `apps/desktop/src/backend/services/storage-service.ts`

### Verification

- `apps/desktop/tests/hooks/queries/useHistoryQuery.test.tsx`
- `apps/desktop/tests/pages/History.test.tsx`

## Explicitly ignored structural facts

- None.
