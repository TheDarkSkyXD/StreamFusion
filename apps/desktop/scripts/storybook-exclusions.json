{
  "version": 1,
  "exclusions": {
    "frontend/App.tsx": "Application composition root that installs providers, router, and globals; it has no isolated visual contract beyond its child surfaces.",
    "frontend/features/auth/components/auth/AuthProvider.tsx": "Authentication lifecycle bootstrap and children wrapper with no standalone visual state or independently catalogable UI.",
    "frontend/features/chat/components/chat/mod/UserPopout/UserPopoutProvider.tsx": "Context and dispatcher wiring whose user-facing surface is the separately storyable UserPopout component.",
    "frontend/features/chat/components/chat/twitch/ModerationFixtureLauncher.tsx": "Development-only query-string status banner gated by import.meta.env.DEV; the production Storybook build cannot activate it without changing its environment contract.",
    "frontend/features/playback/components/player/kick/uptime-readout.tsx": "Imperative ref-driven clock that returns null and only updates a media progress element outside a visual component contract.",
    "frontend/features/playback/components/player/persistent-player-shell.tsx": "Persistent player context registrations and children wrapper with no independently rendered interface to document in Storybook.",
    "frontend/dev-relay/twitch-ad-frame-proof-panel.tsx": "Imperative development proof harness that exposes a mount API rather than a product UI with a stable visual contract.",
    "frontend/features/media-library/components/hooks/use-stream-recording-state.tsx": "State store hook for recording status and live-clock updates; it provides no standalone visual surface to render in Storybook.",
    "frontend/features/moderation/components/screens/Mod/channel/ModChannelKickPage.tsx": "Route adapter that delegates to the shared channel page instead of defining a distinct visual interface.",
    "frontend/features/moderation/components/screens/Mod/channel/ModChannelTwitchPage.tsx": "Route adapter that delegates to the shared channel page instead of defining a distinct visual interface.",
    "frontend/providers/query-provider.tsx": "Application React Query bootstrap provider with development instrumentation rather than a standalone user-facing visual surface.",
    "frontend/renderer.tsx": "DOM bootstrap entry point that mounts the application and has no component surface for Storybook to document.",
    "frontend/routes/router.tsx": "Route-tree configuration that composes actual pages inline rather than exporting a separately storyable visual component."
  }
}
