# Twitch playlist detection fixtures

These HLS playlists are synthetic reconstructions of Twitch playlist structures observed by
StreamFusion. Hosts are synthetic except where a public CDN hostname is itself a detection signal;
identifiers, timestamps, sequence numbers, and paths are synthetic. No raw playlist URL, token,
channel name, user identifier, or full captured segment path is retained.
